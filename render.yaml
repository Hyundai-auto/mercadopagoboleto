services:
  - type: web
    name: appmax-checkout
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: APPMAX_API_KEY
        sync: false
      - key: NODE_VERSION
        value: 18.0.0
